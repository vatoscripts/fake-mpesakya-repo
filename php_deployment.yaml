apiVersion: apps/v1
kind: Deployment
metadata:
  name: php
  labels:
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: php
      tier: backend
  template:
    metadata:
      labels:
        app: php
        tier: backend
    spec:
      volumes:
      - name: dir
        hostPath:
          path: /soo
      containers:
      - name: php
        image: kiyange26773/mpesakya:latest
        volumeMounts:
        - name: dir
          mountPath: /soo
      initContainers:
      - name: install
        image: busybox
        volumeMounts:
        - name: dir
          mountPath: /soo
        command:
        - cp
        - "-r"
        - "/var/www/."
        - "/soo/app"
